ArgumentType.backup_description=Backup all the koans in the src/ for easy restoration later (useful for developing koans).
ArgumentType.backup_args= -backup, backup, b
ArgumentType.class_description=Switch is optional, app tries to find a class definition for any unrecognized string - which becomes a method argument if class is not found. If classcast succeeds - the class will become the only koansuite to run. Permits users/developers to focus on one suite at a time.
ArgumentType.class_args= -class, class, c
ArgumentType.debug_description=Enable debug state in the app.
ArgumentType.debut_args= -debug, debug, d
ArgumentType.default_run_description=Default target. No switch - this runs if no switch is defined, or if a valid class is found as an argument.
ArgumentType.default_run_args=
ArgumentType.duplicated_arg_error_part1=command line arg: 
ArgumentType.duplicated_arg_error_part2=\ is duplicated.
ArgumentType.help_description=Help. Displays stuff to, er, help you.
ArgumentType.help_args= -help, help, h, ?
ArgumentType.method_description=Switch is optional, results from failing to find a class definition by an unrecognized string if switch is omitted.
ArgumentType.method_args= -method, method, m
ArgumentType.restore_description=Restore all the koans in the src/ folder to their original (or last backed up) state.
ArgumentType.restore_args= -reset, reset, r, restore, -restore
ArgumentType.test_description=Run tests. System returns number of failing testcases - not to exceed 255 to retain DOS compatibility for BAT files.
ArgumentType.test_args= -test, test, t
