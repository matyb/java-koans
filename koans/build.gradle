apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'eclipse'

repositories {
   flatDir {
       dirs 'app/lib'
   }
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

dependencies {
   compile name: 'koans'
}